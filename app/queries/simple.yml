main:
  from: payments
  alias: p
  select:
  - operator: sum
    args:
    - i.views
    - i.clicks
    - p.amount
  where:
  - operator: =
    left: user_id
    right: 1
  group:
  - date
  join:
  - from: user_impressions
    alias: i
    cte: true
    

