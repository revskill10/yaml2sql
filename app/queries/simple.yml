---
declare:
  eql:
    operator: =
    left: $1
    value: $2
    
main:
  from: payments
  alias: p
  select:
  - operator: sum
    args:
    - i.views
    - i.clicks
    - p.amount
  where:
  - apply: ["eql", "user_id", "12"]
  group:
  - date
  
    

